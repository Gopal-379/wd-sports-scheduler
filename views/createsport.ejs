<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="copyright" content="©Peddi Gopal">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/styles.css">
    <title><%= title %></title>
</head>
<body class="bg-slate-300">
    <%- include('header') %>
    <div class="grid grid-cols-3">
        <div class="col-start-2 col-span-1">
            <h3 class="text-2xl mt-5">Add a sport</h3>
            <form action="/createsport" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken%>">
                <div class="py-3">
                    <label for="sportName">Enter Name Of Sport:</label>
                    <input id="sportName" name="sportName" type="text" placeholder="Name Of Sport" class="border border-gray-300 text-gray-900 rounded p-2 w-full mt-2" required>
                </div>
                <div class="py-3">
                    <button type="submit" class="text-white bg-blue-500 hover:bg-blue-600 font-medium rounded px-5 py-2">Create</button>
                </div>
            </form>
        </div>
        <div class="py-2">
            <% if(messages && messages.error && messages.error.length>0){ %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                    <li class=" py-2 px-2 border bg-red-200 rounded"> 
                        <%= messages.error[i] %>
                    </li>
                <%} %>
            <%}%>
        </div>
    </div>
</body>
</html>
